pipeline {
    agent { 
        image 'windsekirun/jenkins-android-docker:1.1.1'
    }
       
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
}
